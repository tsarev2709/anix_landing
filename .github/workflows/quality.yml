name: Quality checks

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-build-seo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Tests
        env:
          CI: true
        run: npm test -- --watchAll=false

      - name: Production build and SEO verification
        env:
          CI: false
        shell: bash
        run: |
          set -o pipefail
          npm run build 2>&1 | tee build-output.log

      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: build-output.log
          if-no-files-found: ignore

      - name: Runtime smoke test
        run: node scripts/runtime-smoke-test.js

      - name: Collect production asset metrics
        run: node scripts/report-build-metrics.js

      - name: Upload production asset metrics
        uses: actions/upload-artifact@v4
        with:
          name: build-metrics
          path: artifacts/build-metrics.json
          if-no-files-found: error
