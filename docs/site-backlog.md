# Backlog сайта Anix

## Страница «Как мы работаем»

Будущая отдельная страница с дорожной картой: первая встреча → погружение → исследование → концепция → сценарий → визуальное направление → производство → согласование → адаптации → передача результата.

После создания логично добавить ссылку:

- в блок процесса на главной;
- в footer, раздел «Страницы»;
- на коммерческих страницах `/animation/`, `/ai-video/`, `/medicine/` и `/hse/` рядом с описанием процесса.

В текущую форму и success-popup страницу не добавлять. До её запуска CTA после заявки остаётся `/why_it_works/`.
